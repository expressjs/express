name: SCA Analysis

on:
  push:
    branches:
      - main
  pull_request: {}
  workflow_dispatch:

jobs:
  dependency-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install npm dependencies
        run: npm install

      - name: Install OWASP Dependency Check
        run: |
          wget https://github.com/jeremylong/DependencyCheck/releases/download/v8.1.2/dependency-check-8.1.2-release.zip
          unzip dependency-check-8.1.2-release.zip -d dependency-check
          if [ -f dependency-check/dependency-check/bin/dependency-check.sh ]; then
            chmod +x dependency-check/dependency-check/bin/dependency-check.sh
          else
            echo "Dependency Check installation failed: dependency-check.sh not found."
            exit 1
          fi
      - name: Run OWASP Dependency Check
        run: |
          ddependency-check/ependency-check/bin/dependency-check.sh --project "Express.js Application" --scan . --format "ALL" --out dependency-check-report

      - name: Upload Dependency Check Report
        uses: actions/upload-artifact@v3
        with:
          name: dependency-check-report
          path: dependency-check-report
