#include <iostream>

int main() {
    // std::cout is used to output data to the console (standard output)
    std::cout << "Hello, World!" << std::endl;
    
    // The program returns 0 to indicate successful execution
    return 0;
}
