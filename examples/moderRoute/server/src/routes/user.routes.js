import { Router } from "express"

export const userRoutes = Router();

//? example for using routes eassy way 
userRoutes.route("/userRegistration").post("put here Controller of the any apies ").get("put her  controller ").delete().put();


